// ── Site configuration ──────────────────────────────────────────────
// Replace WHATSAPP_NUMBER with your real WhatsApp business number.
// Format: country code + number, digits only, no +, no spaces, no dashes.
// Example for India: "919876543210"  (91 = country code, then the 10-digit number)
export const WHATSAPP_NUMBER = '8300299667'

export const SITE = {
  name: 'What Next',
  tagline: 'Rise with the right direction',
}
