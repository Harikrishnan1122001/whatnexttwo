# react-scripts 5's bundled eslint-config-react-app has a known conflict with
# newer eslint versions npm may resolve (unknown "jest/globals" env), which
# fails the build. Disabling the CRA eslint plugin avoids that; your editor's
# own ESLint extension will still lint the code as you write it.
DISABLE_ESLINT_PLUGIN=true
